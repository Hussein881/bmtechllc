# Client delivery

Create one directory per client engagement. Keep client names out of directory names when confidentiality requires it; use an agreed project code instead.

Suggested project layout:

```text
project-name/
├── README.md
├── docs/
├── src/
├── tests/
└── deploy/
```

Do not commit credentials, raw client data, contracts, or personally identifiable information.
